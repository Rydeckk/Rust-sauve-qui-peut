[package]
name = "server-rust"
version = "0.1.0"
edition = "2021"

[dependencies]
commun = { path = "../commun" }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["fmt", "env-filter"] }
serde_json = "1.0.139"

